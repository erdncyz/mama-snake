# App Store Connect deliver package

App: Mamba Snake (com.mamba.snake / 6757639534)

## Upload with API key

export ASC_KEY_ID=...
export ASC_ISSUER_ID=...
export ASC_KEY_PATH=/path/to/AuthKey_XXXX.p8

Then run from repo root with fastlane deliver, or the upload script.
